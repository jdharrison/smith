[package]
name = "smith"
version = "0.1.0"
edition = "2021"
authors = ["Jacob D. Harrison"]
description = "Agent Smith — open-source control plane for coding orchestration and configuration"
license = "MIT"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
directories = "6.0"
dirs = "5.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"
reqwest = { version = "0.11", features = ["json"] }
tokio = { version = "1", features = ["full"] }

